<% provide(:title, 'Home') %>
<div class="">
	<% if log_in? %>
		<div class="">
			<% if @friends.empty? %>
				<tr>
					<td>no friends</td>
				</tr>
			<% else %>
				<% @photos.each do |photo| %>
					<tr>
						<td>
							<%= "#{photo}" %>
						</td>
					</tr>
				<% end %>
			<% end %>
		</div>
	<% else %>
		<h1>Welcome!</h1>
		<div id="vk_auth"></div>
		<script type="text/javascript">
			VK.Widgets.Auth("vk_auth", {width: "200px", authUrl:'/signin'});
		</script>
	<% end %>
</div>