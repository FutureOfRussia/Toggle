<% provide(:title, ) %>
<div class="col-sm-9">
<%= @friend.last_name %>
	<% @friend_photos.items.each do |item| %>
		<tr>
			<td>
				<img src="<%= "#{item.photo_604}" %>">
			</td>
		</tr>
	<% end %>
</div>
<div class="col-sm-3">
</div>