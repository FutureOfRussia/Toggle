<div class="">
<% if @friend_photos.error.present? %>
	<%= "#{@friend_photos.error_msg}" %>
<% else %>
	<% @friend_photos.items.each do |item| %>
		<tr>
			<td>
				<img src="<%= "#{item.photo_604}" %>">
			</td>
		</tr>
	<% end %>
<% end %>
</div>