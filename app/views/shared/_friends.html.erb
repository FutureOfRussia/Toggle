<% if @friends.empty? %>
	<li>Друзья не найдены</li>
<% else %>
	<% @friends.items.each do |item| %>
		<li>
			<a href='#' >
				<%= full_name(item) %>
				<%= image_tag(item.photo, class: 'avt-menu img-circle') %>
			</a>
		</li>
	<% end %>
<% end %>