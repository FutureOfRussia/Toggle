<% if @friends.empty? %>
	<li>Друзья не найдены</li>
<% else %>
	<% @friends.items.each do |item| %>
		<li>
			<a href='#' >
				<td align="justify"  ><%= full_name(item) %>
				<%= image_tag(item.photo, class: 'img-circle') %></td>
			</a>
		</li>
	<% end %>
<% end %>