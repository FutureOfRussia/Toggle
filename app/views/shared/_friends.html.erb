<% if @friends.empty? %>
	<li>Друзья не найдены</li>
<% else %>
	<% @friends.items.each do |item| %>
		<li>
			<a href='#' >
				<%= full_name(item) %>
				<div class="avt-menu">
					<%= image_tag(item.photo, class: 'img-circle') %>
				</div>
			</a>
		</li>
	<% end %>
<% end %>