<% if @friends.empty? %>
	<li>Друзья не найдены</li>
<% else %>
	<% @friends.items.each do |item| %>
		<li>
			<a href='#' >
			<div class="row friends-menu">
				<div class="friends-name"><%= full_name(item) %></div>
				<div class="friends-avt"><%= image_tag(item.photo, class: 'img-circle') %></div>
			</div>
			</a>
		</li>
	<% end %>
<% end %>